# Строки, которые ОБЯЗАНЫ присутствовать в <head> главной страницы собранного релиза.
# deploy.sh проверяет их ДО переключения: если строки нет — релиз не выкатывается,
# прод остаётся на текущей версии. Одна строка = один маркер, # — комментарий.
#
# Зачем: эти вещи уже дважды молча терялись на проде (выкладка из ветки, где их
# не было; устаревший пререндер из .next/cache). Если маркер убирается намеренно —
# уберите его здесь в том же коммите.

# Счётчик AdRiver (требование клиента: скрипт строго в <head>)
sid:231322
# Подтверждение прав Google Search Console
google-site-verification
